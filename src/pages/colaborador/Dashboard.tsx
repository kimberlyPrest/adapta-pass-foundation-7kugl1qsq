import { SectionTitle } from '@/components/ui/section-title'
export default function ColaboradorDashboard() {
  return <SectionTitle title="Portal do Colaborador" subtitle="Minhas atividades e processos." />
}
